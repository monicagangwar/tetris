Dependencies
  1.Pygame
	  Installation : sudo apt-get install python-pygame

Run command
  python tetris.py

How to play
  Left Arrow key  : move block to left
  Right Arrow key : move block to right
  Tap Down key    : speed up the block
  Space	key       : rotate block
